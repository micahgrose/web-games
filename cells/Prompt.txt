Alright. Cells is updated well. I want 1 things done here:
   1 : Ensure that whenever right click is just clicked or dragged, that the default options that appear (inspect, veiw page source, etc.) are not shown.
   2 : On the dropdown menu that shows previous saves, make it so when an option is selected the topbar stays open untill the mouse re-enters the top bar and then leaves again, at which point the top bar can retract to the top of the screen.
   3 : Give the game a better name
   4 : Delete any titles called "Cells" and replace them with that name.
   5 : Create a tutorial that walks the user through how to use the game.
       A : Include 6 demonstrations of cells which should be
            1 : A pusher moving
            2 : A pusher pushing a pushable block
            3 : A rotator that rotates the pusher
            4 : A pusher running into an unmovable block
            5 : A generator generating moveable blocks
            6 : A pusher pushing 5 moveable blocks into a deleter
       B : How you will set up the tutorial is like this
           1 : No hotkeys, placing, or clicking of any buttons is allowed accept on a confirm prompt that asks if the user wants to start the tutorial or skip.
           2 : If skipped, all functionality is restored, and the demosntrations are not shown.
           3 : If not skipped, then all functionality is still held back, and the demonstrations will start.
           4 : Demonstrations 1, place a pusher in the middle of the screen. Tell the user about the pusher. prompt the player to click start. Have the pusher advance 5 steps before moving on to demonstration 2.
           5 : Demonstration 2, place a pusher in the middle pushing a pushable block. Tell the user about the pushable blocks. Prompt the player to click start. Advance 5 steps.
           6 : Demonstration 3. Place a pusher in the middle of the screen with a rotator 2 steps in front of it's path. Advance 5 steps. Etc.
           7 : Continue through all 5 demonstrations in the same fashion.
           8 : When the demonstration is over, restore all functionalilty and remove all tutorial elements.
    6 : Wipe my local storage.
    7 : Clean up the save/load function by tying saves to a players account, not localstorage.
    8 : Move, rename and change any files in order to get Cells (or whatever you call it) to be in RoseGarden and all connected up.

Additionally, wipe all current RoseGarden accounts. Ensure that the website can reach the RoseGardenIcon.png. New accounts should be verified by a six digit code sent to their email. When logging in, there should be a forgot password option where you can type in your username and an 8 digit confirmation code will be sent to the attached email of that username. The user then enters this code and another email is sent to them with their password.

Push and merge and all that stuff.